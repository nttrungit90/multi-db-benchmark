server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: none
    open-in-view: false
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3307}/benchmark?rewriteBatchedStatements=true&useServerPrepStmts=true
    username: root
    password: bench123
    hikari:
      maximum-pool-size: 20
  data:
    mongodb:
      uri: mongodb://${MONGODB_HOST:localhost}:${MONGODB_PORT:27018}/benchmark

benchmark:
  data-dir: ${DATA_DIR:/app/data}
